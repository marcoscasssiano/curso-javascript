const numeros = [1,2,3,4,5];
 const juntando =  nnumeros.join(".")
console.log(juntando);

const frase = "ola bem vindo ao curso";
const separandoStrings = frase.split(" ")
console.log(separandoStrings);
